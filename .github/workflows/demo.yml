name: Manual Trigger Workflow with username
#get username information
on:
  push:
  workflow_dispatch:
    inputs:
      choice:
        description: 'Deploy to Production?'
        default: ''
        required: true
        type: string

jobs:
  print-username:
    runs-on: ubuntu-latest
    steps:
      - name: Print username
        run: |
          echo "The username selected option: ${{github.event.inputs.choice }}"
